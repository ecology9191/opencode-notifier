# Agent Notes

This repository is a fork and Telegram-focused package variant of `mohak34/opencode-notifier`.

If `origin` points at `https://github.com/mohak34/opencode-notifier.git`, push may fail for agents without upstream write access. Use the maintainer's fork remote or ask for the correct push target instead of assuming upstream access.
